import { PipefiltroPersonaPipe } from './pipefiltro-persona.pipe';

describe('PipefiltroPersonaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipefiltroPersonaPipe();
    expect(pipe).toBeTruthy();
  });
});
